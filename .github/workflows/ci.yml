name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  test:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install linter
        run: pip install ruff

      - name: Lint (ruff)
        run: ruff check src/ tests/

      - name: Run tests
        run: pytest tests/ -v --tb=long 2>&1 | tee /tmp/test_output.txt; exit ${PIPESTATUS[0]}
        env:
          API_KEY: test-key-for-unit-tests-ci-safe-32chars
          MODEL_VERSION: v1.0.0
          EMBEDDING_MODEL: intfloat/multilingual-e5-base
          DEVICE: cpu
          PYTHONPATH: .

      - name: Show failures (always)
        if: failure()
        run: grep -A 30 "FAILED\|ERROR\|short test summary" /tmp/test_output.txt || cat /tmp/test_output.txt | tail -80
